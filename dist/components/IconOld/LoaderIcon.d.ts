import React from 'react';
import { IconProps } from '../../types/icon';
declare const LoaderIcon: ({ color, size, strokeWidth, className, }: IconProps) => React.JSX.Element;
export default LoaderIcon;
